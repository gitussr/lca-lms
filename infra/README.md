# infra/

Local and deployment infrastructure for LCA LMS
(see `docs/LCA LMS — Master Project Prompt v1.0.md` §15, §33).

> **Not scaffolded yet.**
> - **F-008 — Local development environment** adds `docker-compose.yml` (PostgreSQL + Redis)
>   and the first-admin seed instructions.
> - **F-009 — CI pipeline** adds the CI workflow (lint → typecheck → build → tests).

No production deployment manifests live here until a deployment architecture decision is
made (Master Prompt §36).
