# frontend/

The LCA LMS web client — React + TypeScript
(see `docs/LCA LMS — Master Project Prompt v1.0.md` §15, §17).

> **Not scaffolded yet.** Feature **F-007 — Frontend application skeleton** creates the
> React app, routing (public vs protected routes), the central `/api/v1` API client, and
> the Student / Teacher / Admin layout shells.

Talks only to the backend REST API under `/api/v1` — never directly to the database
(Master Prompt §17).
